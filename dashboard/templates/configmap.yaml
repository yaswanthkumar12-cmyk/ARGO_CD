apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-html
  namespace: {{ .Release.Namespace }}
data:
  index.html: |-
    {{ .Values.htmlContent | replace "{{ .Values.dashboardVersion }}" .Values.dashboardVersion | indent 4 }}
