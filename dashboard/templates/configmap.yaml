apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-html
  namespace: {{ .Release.Namespace }}
data:
  index.html: |-
{{ .Values.htmlContent | replace "DASHBOARD_VERSION" .Values.dashboardVersion | nindent 4 }}
